version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: events_calndar_frontend
    networks:
      -mysibsau
      -default

networks:
  mysibsau:
    external: true